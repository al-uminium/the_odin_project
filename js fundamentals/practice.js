const add7 = num => num+7;

const multiply = (a,b) => a*b;

const capitalize = (string) => string.toUpperCase()[0] + string.toLowerCase().slice(1);

const lastLetter = (string) => string.charAt(string.length-1)

let a = lastLetter('TESTaaa')
console.log(a)